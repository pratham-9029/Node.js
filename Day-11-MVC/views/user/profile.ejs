<%- include('../partials/head') %>
    <%- include('../partials/navbar') %>

        <div class="container section">
            <h1 class="section-title" style="font-size:1.8rem; margin-bottom:2rem;">👤 My Profile</h1>
            <div style="max-width:600px;">
                <div class="review-card">
                    <div class="review-header" style="margin-bottom:1.5rem;">
                        <div class="review-avatar" style="width:60px; height:60px; font-size:1.4rem;">
                            <%= currentUser.username.charAt(0).toUpperCase() %>
                        </div>
                        <div>
                            <div class="username" style="font-size:1.1rem;">
                                <%= currentUser.username %>
                            </div>
                            <div class="date">
                                <%= currentUser.email %>
                            </div>
                        </div>
                    </div>
                    <h3 style="font-family:var(--font-heading); margin-bottom:1rem;">📊 Watchlist Stats</h3>
                    <div class="stats-grid" style="grid-template-columns:repeat(auto-fit, minmax(130px,1fr));">
                        <% const statusMap={plan_to_watch:'📌 Plan to Watch', watching:'▶️ Watching', completed:'✅
                            Completed', dropped:'❌ Dropped', on_hold:'⏸️ On Hold'}; %>
                            <% Object.keys(statusMap).forEach(key=> {
                                const found = statusData.find(s => s._id === key); %>
                                <div class="stat-card" style="padding:1rem;">
                                    <div class="stat-value" style="font-size:1.5rem;">
                                        <%= found ? found.count : 0 %>
                                    </div>
                                    <div class="stat-label" style="font-size:0.75rem;">
                                        <%= statusMap[key] %>
                                    </div>
                                </div>
                                <% }) %>
                    </div>
                </div>
            </div>
        </div>

        <%- include('../partials/footer') %>