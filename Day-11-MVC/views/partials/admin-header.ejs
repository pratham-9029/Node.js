<%- include('../partials/head') %>
    <div class="admin-layout">
        <aside class="admin-sidebar">
            <div class="sidebar-logo">🎬 CineVerse</div>
            <ul class="sidebar-nav">
                <li><a href="/admin"
                        class="<%= typeof title !== 'undefined' && title.includes('Dashboard') ? 'active' : '' %>"><span
                            class="icon">📊</span> Dashboard</a></li>
                <li><a href="/admin/movies"
                        class="<%= typeof title !== 'undefined' && title.includes('Manage Movies') ? 'active' : '' %>"><span
                            class="icon">🎬</span> Movies</a></li>
                <li><a href="/admin/movies/add"
                        class="<%= typeof title !== 'undefined' && title.includes('Add Movie') ? 'active' : '' %>"><span
                            class="icon">➕</span> Add Movie</a></li>
                <li><a href="/admin/users"
                        class="<%= typeof title !== 'undefined' && title.includes('Users') ? 'active' : '' %>"><span
                            class="icon">👥</span> Users</a></li>
                <li><a href="/admin/reviews"
                        class="<%= typeof title !== 'undefined' && title.includes('Reviews') ? 'active' : '' %>"><span
                            class="icon">📝</span> Reviews</a></li>
            </ul>
            <div style="padding:1.5rem; margin-top:auto; border-top:1px solid var(--border);">
                <div style="font-size:0.8rem; color:var(--text-muted); margin-bottom:0.5rem;">Signed in as</div>
                <div style="font-size:0.9rem; font-weight:600;">
                    <%= currentUser.username %>
                </div>
                <div style="display:flex; gap:0.5rem; margin-top:0.8rem;">
                    <a href="/" class="btn btn-ghost btn-sm" style="flex:1; justify-content:center;">🌐 Site</a>
                    <a href="/auth/logout" class="btn btn-ghost btn-sm" style="flex:1; justify-content:center;">🚪
                        Logout</a>
                </div>
            </div>
        </aside>
        <main class="admin-main">