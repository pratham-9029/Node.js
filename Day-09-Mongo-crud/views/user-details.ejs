<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .user-info { background-color: #f9f9f9; padding: 20px; border-radius: 5px; }
        .back-link { margin-bottom: 20px; }
        .btn { padding: 8px 16px; text-decoration: none; border-radius: 4px; margin-right: 10px; }
        .btn-edit { background-color: #007bff; color: white; }
        .btn-delete { background-color: #dc3545; color: white; }
        .btn-back { background-color: #6c757d; color: white; }
    </style>
</head>
<body>
    <div class="back-link">
        <a href="/users" class="btn btn-back">← Back to Users</a>
    </div>
    
    <div class="user-info">
        <h1>User Details</h1>
        <p><strong>ID:</strong> <%= user._id %></p>
        <p><strong>Name:</strong> <%= user.name %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Age:</strong> <%= user.age %></p>
        <p><strong>Created At:</strong> <%= user.createdAt ? new Date(user.createdAt).toLocaleString() : '' %></p>
    </div>
    
    <div style="margin-top: 20px;">
        <a href="/users/edit/<%= user._id %>" class="btn btn-edit">Edit User</a>
        <form method="POST" action="/users/<%= user._id %>?_method=DELETE" style="display: inline-block;">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this user?')">Delete User</button>
        </form>
    </div>
</body>
</html>